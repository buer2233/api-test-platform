# 详细测试用例说明书（V1）
## 1. 文档说明
- **文档名称**：详细测试用例说明书（V1）
- **所属项目**：接口自动化测试平台
- **文档目标**：定义 V1 阶段需要覆盖的详细测试用例，用于指导后续 TDD 开发、测试实现、阶段验证与验收。
- **适用范围**：V1 范围内的架构底座、中间模型、模板体系、规则体系、文档驱动最小闭环、执行闭环
- **文档状态**：第一版（持续维护）
- **关联文档**：
  - `product_document/产品需求说明书(全局).md`
  - `product_document/架构设计/总体架构设计说明书.md`
  - `product_document/架构设计/中间模型设计说明书.md`
  - `product_document/架构设计/现有接口自动化测试框架改造方案.md`
  - `product_document/阶段文档/V1阶段工作计划文档.md`
  - `product_document/架构设计/模板体系与代码生成规范说明书.md`
---
## 2. V1 测试目标
V1 测试的目标不是验证最终完整产品，而是验证 V1 最小闭环是否成立。
V1 测试重点验证以下内容：
1. 说明书约束是否可落地；
2. 中间模型是否能稳定承接文档输入；
3. 模板体系是否能生成稳定资产；
4. 规则体系是否能发现基础不合规问题；
5. 当前框架是否能承接生成资产，并能基于统一公开测试站点完成接口验证；
6. pytest 执行最小闭环是否成立；
7. 生成记录与执行记录是否具备最小可追溯能力。
---
## 2.1 当前公开接口测试基线站点
当前 V1 阶段涉及公开接口自动化测试、底座验证示例和回归用例设计时，统一使用：
- `https://jsonplaceholder.typicode.com/`
测试设计必须遵循其公开 Guide 约束：
1. 资源范围固定在 `posts/comments/albums/photos/todos/users` 等示例资源；
2. 支持 `GET/POST/PUT/PATCH/DELETE`；
3. 支持查询参数过滤与一层嵌套路由；
4. `POST/PUT/PATCH/DELETE` 为伪写入，不做真实持久化，断言应验证返回契约而不是持久化结果。
---
## 3. 测试设计原则
## 3.1 说明书驱动原则
所有测试用例必须可追溯到产品说明书与 V1 说明书中的明确目标。
## 3.2 TDD 原则
后续实现阶段必须优先按本说明书中的测试项编写测试，再进行实现。
## 3.3 最小闭环优先原则
V1 优先保证最小闭环成立，而不是追求覆盖未来所有能力。
## 3.4 分层测试原则
测试用例应覆盖：
- 文档/设计层校验
- 模型层
- 模板层
- 规则层
- 执行层
- 集成闭环层
## 3.5 可维护原则
测试用例说明应清晰、编号稳定、便于阶段更新。
---
## 4. 测试分层结构
建议 V1 测试分为以下六层：
1. **文档约束测试**
2. **中间模型测试**
3. **模板生成测试**
4. **规则校验测试**
5. **执行与记录测试**
6. **最小闭环集成测试**
---
## 5. 文档约束测试用例
## 5.1 目标
验证 V1 的设计输出是否仍然符合当前已确认的产品边界与架构原则。
### TC-V1-DOC-001
- **名称**：V1 范围不应越界到完整三路线实现
- **目标**：验证 V1 设计与实现计划不包含“一次性完整实现文档驱动、功能用例驱动、抓包驱动全部成品能力”的越界内容。
- **前置条件**：已有 V1 范围说明书
- **检查点**：
 1. V1 范围中明确“文档驱动最小闭环”为主线；
 2. 功能用例驱动与抓包驱动列为后续增强方向；
 3. 不将 V1 宣称为最终产品成品版。
- **预期结果**：通过。
### TC-V1-DOC-002
- **名称**：V1 必须围绕统一中间模型设计
- **目标**：验证当前设计未绕过中间模型直接进入生成实现。
- **检查点**：
 1. 中间模型设计文档存在；
 2. 相关架构说明中已明确“输入 → 中间模型 → 模板/规则 → 生成资产”的路径。
- **预期结果**：通过。
### TC-V1-DOC-003
- **名称**：V1 必须采用模板化生成策略
- **目标**：验证平台没有把自由文本生成直接定义为最终落地方案。
- **检查点**：
 1. 模板体系文档存在；
 2. 全局产品说明书中已明确模板生成原则；
 3. AGENTS.md 中已明确禁止直接自由生成最终生产代码。
- **预期结果**：通过。
### TC-V1-DOC-004
- **名称**：公开接口测试基线必须统一使用 JSONPlaceholder
- **目标**：验证当前公开接口自动化测试、底座演示和回归示例未继续分散到多个临时站点。
- **检查点**：
 1. `AGENTS.md` 中已明确 JSONPlaceholder 为当前专用公开测试站点；
 2. `api_test` 公开示例和相关测试文档已切换到该站点；
 3. 写操作断言未错误假设持久化成功。
- **预期结果**：通过。
### TC-V1-DOC-005
- **名称**：`api_test` 公开示例应符合 JSONPlaceholder Guide 的测试方法
- **目标**：验证底座公开示例已覆盖该站点允许的核心测试路径，而不是继续停留在无关公开网站。
- **检查点**：
 1. 至少覆盖单资源读取、过滤查询、嵌套路由；
 2. 至少覆盖 `POST/PUT/PATCH/DELETE` 的伪写入契约；
 3. 底座请求层可兼容 `201` 等标准 REST 状态码。
- **预期结果**：通过。
### TC-V1-DOC-006
- **名称**：`api_test` 公开测试底座应提供可复用的 JSONPlaceholder 资源与 fixture
- **目标**：验证公开测试不只是零散示例，而是已经具备复用的资源级 API 封装和公共 fixture。
- **检查点**：
 1. 存在可复用的 JSONPlaceholder API 封装；
 2. 存在公共 fixture 供多文件复用；
 3. 至少覆盖 users 和 todos 两类资源。
- **预期结果**：通过。
---
## 6. 中间模型测试用例
## 6.1 目标
验证中间模型是否能稳定承接文档输入，并为模板层和规则层提供结构化信息。
### TC-V1-MODEL-001
- **名称**：SourceDocument 模型应能承接文档来源信息
- **目标**：验证输入源可被统一记录。
- **输入**：一个 OpenAPI 文档来源信息
- **检查点**：
 1. 能记录 source_id；
 2. 能记录 source_type；
 3. 能记录来源路径或引用；
 4. 能记录导入时间。
- **预期结果**：通过。
### TC-V1-MODEL-002
- **名称**：ApiModule 模型应能表达模块归属
- **目标**：验证接口模块归类信息可被结构化表达。
- **输入**：模块名称、模块类型、路径提示
- **检查点**：
 1. 可记录 module_name；
 2. 可记录 module_type；
 3. 可记录路径映射提示。
- **预期结果**：通过。
### TC-V1-MODEL-003
- **名称**：ApiOperation 模型应能表达基础接口定义
- **目标**：验证单接口定义可完整表达。
- **输入**：接口方法、路径、参数、响应结构摘要
- **检查点**：
 1. 可记录 http_method；
 2. 可记录 path；
 3. 可记录 operation_name；
 4. 可记录基础参数信息；
 5. 可记录 success_codes。
- **预期结果**：通过。
### TC-V1-MODEL-004
- **名称**：ApiParam 模型应能表达不同参数位置
- **目标**：验证 header/query/path/body 参数都可表达。
- **输入**：4类不同参数定义
- **检查点**：
 1. 能区分 param_in；
 2. 能记录 required；
 3. 能记录数据类型；
 4. 能记录默认值或样例值。
- **预期结果**：通过。
### TC-V1-MODEL-005
- **名称**：ResponseField 模型应能表达断言候选来源
- **目标**：验证响应字段后续可为断言和变量提取提供基础。
- **输入**：响应字段定义
- **检查点**：
 1. 能记录字段路径；
 2. 能记录字段类型；
 3. 能标记是否可断言；
 4. 能标记是否可提取变量。
- **预期结果**：通过。
### TC-V1-MODEL-006
- **名称**：GenerationRecord 模型应能记录最小生成元数据
- **目标**：验证生成行为可追溯。
- **检查点**：
 1. 能记录生成时间；
 2. 能记录来源；
 3. 能记录目标文件；
 4. 能记录生成策略。
- **预期结果**：通过。
### TC-V1-MODEL-006A
- **名称**：GenerationRecord 模型应能记录资产归属与摘要信息
- **目标**：验证生成记录不仅可追溯，还可直接被服务层用于展示生成结果。
- **检查点**：
 1. 能记录 `module_code`；
 2. 能记录 `operation_code`；
 3. 能记录 `target_asset_digest`。
- **预期结果**：通过。
### TC-V1-MODEL-007
- **名称**：ExecutionRecord 模型应能记录最小执行信息
- **目标**：验证执行行为可追溯。
- **检查点**：
 1. 能记录执行级别；
 2. 能记录执行结果；
 3. 能记录开始和结束时间；
 4. 能关联报告路径。
- **预期结果**：通过。
### TC-V1-MODEL-007A
- **名称**：ExecutionRecord 模型应能记录结构化执行摘要
- **目标**：验证执行记录可直接表达命令、退出码和测试数量统计。
- **检查点**：
 1. 能记录 `command`；
 2. 能记录 `exit_code`；
 3. 能记录 `total/passed/failed/error/skipped` 统计字段。
- **预期结果**：通过。
### TC-V1-MODEL-008
- **名称**：RouteCapabilitySummary 模型应能表达路线状态与阻断原因
- **目标**：验证服务层能力快照中的单路线摘要可直接被外部入口消费。
- **检查点**：
 1. 能记录 `route_code`；
 2. 能记录 `enabled`；
 3. 能记录 `stage`；
 4. 能记录 `detail`。
- **预期结果**：通过。
### TC-V1-MODEL-009
- **名称**：DocumentPipelineRunSummary 模型应能表达文档驱动运行摘要
- **目标**：验证服务层可稳定输出文档驱动运行结果，而不是直接暴露底层流水线结构。
- **检查点**：
 1. 能记录 `route_code` 与 `service_stage`；
 2. 能记录 `source/source_id/source_type/workspace_root`；
 3. 能记录 `generation_count/asset_count/asset_type_breakdown`；
 4. 能记录 `execution_id`、执行状态、退出码与测试数量统计。
- **预期结果**：通过。
### TC-V1-MODEL-010A
- **名称**：WorkspaceAssetInventorySummary 模型应能表达工作区资产聚合摘要
- **目标**：验证服务层可稳定输出资产类型、生成类型和执行状态聚合信息。
- **检查点**：
 1. 能记录 `asset_type_breakdown`；
 2. 能记录 `generation_type_breakdown`；
 3. 能记录 `generation_review_status_breakdown`；
 4. 能记录 `generation_execution_status_breakdown`。
- **预期结果**：通过。
### TC-V1-MODEL-010
- **名称**：WorkspaceInspectionSummary 模型应能表达工作区检查摘要
- **目标**：验证服务层可稳定输出工作区检查结果，并直接表达问题数量与摘要列表。
- **检查点**：
 1. 能记录 `command_code` 与 `service_stage`；
 2. 能记录 `workspace_root/manifest_path/source_id/source_type/execution_id`；
 3. 能记录 `missing_asset_count/missing_generation_record_count/digest_mismatch_count/validation_error_count`；
 4. 能记录 `inventory_summary`、资产摘要、生成记录摘要与问题列表。
- **预期结果**：通过。
---
## 7. 模板生成测试用例
## 7.1 目标
验证模板体系能够根据中间模型稳定生成统一风格的资产。
### TC-V1-TPL-001
- **名称**：接口方法模板应能生成标准接口代码骨架
- **目标**：验证单个 ApiOperation 可生成统一风格的接口方法代码。
- **输入**：一个完整的 ApiOperation 模型实例
- **检查点**：
 1. 文件内容包含统一 import；
 2. 生成的方法命名符合规则；
 3. 请求方法与路径正确；
 4. 参数组织符合模板约定；
 5. 返回结构符合模板约定。
- **预期结果**：通过。
### TC-V1-TPL-002
- **名称**：pytest 用例模板应能生成基础测试骨架
- **目标**：验证接口测试骨架可被模板稳定输出。
- **输入**：一个 ApiOperation + AssertionCandidate
- **检查点**：
 1. 生成 `test_*.py` 风格文件；
 2. 包含基础 fixture 引用；
 3. 包含接口调用示例；
 4. 包含状态码断言；
 5. 包含基础响应断言占位。
- **预期结果**：通过。
### TC-V1-TPL-002A
- **名称**：pytest 用例模板应能为对象结构断言生成匹配的假响应
- **目标**：验证测试骨架中的伪客户端不再依赖固定返回值，而是能根据对象类断言生成最小可执行响应。
- **输入**：包含 `status_code`、`json_field_exists`、`schema_match(type=object)` 的 `AssertionCandidate` 集合。
- **检查点**：
 1. 生成结果包含对象结构的 `data` 节点；
 2. `required_fields` 中的关键字段会出现在假响应中；
 3. 假响应内容可直接支撑同文件中的断言片段执行。
- **预期结果**：通过。
### TC-V1-TPL-002B
- **名称**：pytest 用例模板应能为数组结构断言生成匹配的假响应
- **目标**：验证数组型接口生成测试骨架时，不会再错误写死对象结构的假响应。
- **输入**：包含 `status_code`、`json_field_exists`、`schema_match(type=array)` 的 `AssertionCandidate` 集合。
- **检查点**：
 1. 生成结果中的目标路径值为数组；
 2. 假响应结构与数组断言保持一致；
 3. 生成测试可直接用于最小 smoke 执行。
- **预期结果**：通过。
### TC-V1-TPL-002C
- **名称**：pytest 用例模板应能为对象数组结构断言生成匹配的假响应
- **目标**：验证对象数组场景下，伪客户端能生成最小对象数组，而不是空列表或错误对象结构。
- **输入**：包含 `schema_match(type=array,item_type=object,required_fields=...)` 的 `AssertionCandidate` 集合。
- **检查点**：
 1. 生成结果中的目标路径值为对象数组；
 2. 数组项包含 `required_fields` 对应字段；
 3. 假响应内容可直接支撑数组项结构断言执行。
- **预期结果**：通过。
### TC-V1-TPL-002D
- **名称**：pytest 用例模板应能为 `business_rule(non_empty_string)` 生成匹配的假响应
- **目标**：验证业务断言进入模板闭环后，伪客户端能自动补齐满足非空字符串规则的最小响应值。
- **输入**：包含 `status_code` 和 `business_rule(rule_code=non_empty_string)` 的 `AssertionCandidate` 集合。
- **检查点**：
 1. 生成结果中的目标路径值为非空字符串；
 2. 假响应结构与 `target_path` 保持一致；
 3. 假响应内容可直接支撑同文件中的 `business_rule` 断言执行。
- **预期结果**：通过。
### TC-V1-TPL-002E
- **名称**：pytest 用例模板应能为 `business_rule(positive_integer)` 生成匹配的假响应
- **目标**：验证业务断言进入模板闭环后，伪客户端能自动补齐满足正整数规则的最小响应值。
- **输入**：包含 `status_code` 和 `business_rule(rule_code=positive_integer)` 的 `AssertionCandidate` 集合。
- **检查点**：
 1. 生成结果中的目标路径值为正整数；
 2. 假响应结构与 `target_path` 保持一致；
 3. 假响应内容可直接支撑同文件中的 `business_rule` 断言执行。
- **预期结果**：通过。
### TC-V1-TPL-003
- **名称**：断言模板应能生成统一断言片段
- **目标**：验证状态码/字段断言能被模板统一输出。
- **输入**：不同类型的 AssertionCandidate
- **检查点**：
 1. 状态码断言格式统一；
 2. JSON 字段断言格式统一；
 3. 断言错误信息表达清晰。
- **预期结果**：通过。
### TC-V1-TPL-003A
- **名称**：`schema_match` 断言模板应能生成稳定的结构断言片段
- **目标**：验证 `schema_match` 在 V1 的最小实现可稳定渲染，并保持字段列表字面量可预测。
- **输入**：`AssertionCandidate(assertion_type=schema_match, expected_value={"type":"object","required_fields":["id","name"]})`
- **检查点**：
 1. 生成结果包含目标路径读取逻辑；
 2. 生成结果包含类型校验逻辑；
 3. `required_fields` 以稳定的 JSON 风格列表渲染。
- **预期结果**：通过。
### TC-V1-TPL-003B
- **名称**：`schema_match` 断言模板应能生成对象数组结构断言片段
- **目标**：验证 `schema_match` 在对象数组场景下可渲染数组项类型和字段校验逻辑。
- **输入**：`AssertionCandidate(assertion_type=schema_match, expected_value={"type":"array","item_type":"object","required_fields":["id","name"]})`
- **检查点**：
 1. 生成结果包含数组遍历逻辑；
 2. 生成结果包含数组项对象类型校验；
 3. 生成结果包含数组项字段存在性校验。
- **预期结果**：通过。
### TC-V1-TPL-003C
- **名称**：`business_rule` 断言模板应能生成稳定的最小业务断言片段
- **目标**：验证 `business_rule(non_empty_string)` 在 V1 的最小实现可稳定渲染为可执行断言代码。
- **输入**：`AssertionCandidate(assertion_type=business_rule, target_path="data.name", expected_value={"rule_code":"non_empty_string"})`
- **检查点**：
 1. 生成结果包含目标路径读取逻辑；
 2. 生成结果包含字符串类型校验；
 3. 生成结果包含非空字符串校验和明确的 `business_rule` 错误信息。
- **预期结果**：通过。
### TC-V1-TPL-003D
- **名称**：`business_rule(positive_integer)` 断言模板应能生成稳定的正整数断言片段
- **目标**：验证 `business_rule(positive_integer)` 可稳定渲染为可执行断言代码。
- **输入**：`AssertionCandidate(assertion_type=business_rule, target_path="data.total", expected_value={"rule_code":"positive_integer"})`
- **检查点**：
 1. 生成结果包含目标路径读取逻辑；
 2. 生成结果包含整数类型校验；
 3. 生成结果包含大于零校验和明确的 `business_rule` 错误信息。
- **预期结果**：通过。
### TC-V1-TPL-004
- **名称**：生成记录模板应能生成最小记录结构
- **目标**：验证生成记录可被稳定落盘。
- **检查点**：
 1. 可记录来源；
 2. 可记录生成时间；
 3. 可记录目标资产路径；
 4. 可记录审核状态占位。
- **预期结果**：通过。
### TC-V1-TPL-005
- **名称**：响应示例应可生成 `json_field_equals` 断言模板片段
- **目标**：验证模板层能够根据响应示例值输出稳定的 JSON 字段相等断言。
- **输入**：包含响应字段示例值的 `AssertionCandidate(assertion_type=json_field_equals)`。
- **检查点**：
 1. 断言片段可正确读取嵌套 JSON 路径；
 2. 断言值与输入示例保持一致；
 3. 渲染结果可直接嵌入 pytest 测试骨架。
- **预期结果**：通过。
---
## 8. 规则校验测试用例
## 8.1 目标
验证基础规则体系能发现明显不合规生成结果。
### TC-V1-RULE-001
- **名称**：接口方法命名不符合规范时应被识别
- **目标**：验证规则层可检查接口方法命名。
- **输入**：不符合命名规范的接口方法定义
- **预期结果**：规则校验失败，返回命名不合规提示。
### TC-V1-RULE-002
- **名称**：测试文件命名不符合规范时应被识别
- **目标**：验证测试文件命名规则生效。
- **输入**：非 `test_*.py` 文件名
- **预期结果**：规则校验失败。
### TC-V1-RULE-003
- **名称**：模块归类缺失时应被识别
- **目标**：验证模块归档规则生效。
- **输入**：缺失 module_id / module_name 的操作对象
- **预期结果**：规则校验失败。
### TC-V1-RULE-004
- **名称**：接口核心字段缺失时应被识别
- **目标**：验证接口定义必要字段检查生效。
- **输入**：缺少 method/path 的操作对象
- **预期结果**：规则校验失败。
### TC-V1-RULE-005
- **名称**：生成记录缺少基础追溯字段时应被识别
- **目标**：验证 `GenerationRecord` 至少具备 `source_ids`、`target_asset_path`、`template_reference` 和 `generation_version`。
- **输入**：缺失上述字段之一的生成记录对象。
- **预期结果**：规则校验失败，并返回缺失字段提示。
### TC-V1-RULE-006
- **名称**：AI 辅助生成记录缺少 `prompt_reference` 时应被识别
- **目标**：验证 AI 辅助生成结果不会绕过最小追溯要求。
- **输入**：`generator_type=ai_assisted` 且未设置 `prompt_reference` 的生成记录对象。
- **预期结果**：规则校验失败，并返回 `prompt_reference` 缺失提示。
### TC-V1-RULE-007
- **名称**：可执行接口缺少 `status_code` 断言时应被识别
- **目标**：验证执行前的断言集合至少包含基础状态码校验。
- **输入**：包含 `success_codes` 但断言列表中无 `status_code` 的接口对象。
- **预期结果**：规则校验失败。
### TC-V1-RULE-008
- **名称**：资产清单缺少基础关联字段时应被识别
- **目标**：验证 `asset_manifest` 至少具备资产列表、生成记录关联、执行记录关联与报告路径。
- **输入**：缺失 `assets`、`generation_ids`、`execution_id` 或 `report_path` 的资产清单对象。
- **预期结果**：规则校验失败。
### TC-V1-RULE-009
- **名称**：规则层不应再暴露旧专用站点规则入口
- **目标**：验证通用框架已删除与历史专用站点绑定的规则校验方法。
- **输入**：`RuleValidator` 实例。
- **预期结果**：不存在旧专用规则入口，只保留通用规则校验方法。
### TC-V1-RULE-010
- **名称**：已删除的历史专用文件不应重新出现在仓库
- **目标**：验证通用框架重构完成后，旧配置、旧桥接和账号文件不会被再次带回当前仓库。
- **输入**：仓库根目录。
- **预期结果**：历史专用文件不存在。
### TC-V1-RULE-011
- **名称**：`schema_match` 缺少最小结构时应被识别
- **目标**：验证规则层能拦截不完整或不合法的 `schema_match` 配置。
- **输入**：`expected_value` 缺少 `type` 或 `required_fields` 结构非法的 `AssertionCandidate`。
- **检查点**：
 1. 缺少 `type` 时返回校验失败；
 2. 非法 `required_fields` 时返回校验失败；
 3. 违规信息中明确指出 `schema_match` 配置问题。
- **预期结果**：通过。
### TC-V1-RULE-012
- **名称**：对象数组 `schema_match` 的非法 `item_type` 应被识别
- **目标**：验证规则层能拦截对象数组结构中不合法的 `item_type` 或不匹配的字段配置。
- **输入**：`expected_value={"type":"array","item_type":"unsupported","required_fields":["id"]}` 的 `AssertionCandidate`。
- **检查点**：
 1. 非法 `item_type` 返回校验失败；
 2. 违规信息中明确指出 `schema_match.item_type`；
 3. 对象数组字段规则不会被静默忽略。
- **预期结果**：通过。
### TC-V1-RULE-013
- **名称**：非法 `business_rule` 配置应被识别
- **目标**：验证规则层能拦截缺失或不支持的 `business_rule.rule_code`。
- **输入**：`AssertionCandidate(assertion_type=business_rule, expected_value={"rule_code":"unsupported"})`
- **检查点**：
 1. 非法 `rule_code` 返回校验失败；
 2. 违规信息中明确指出 `business_rule`；
 3. 不支持的业务断言不会被静默忽略。
- **预期结果**：通过。
---
## 9. 执行与记录测试用例
## 9.1 目标
验证生成资产能进入最小执行闭环，并保留执行记录。
### TC-V1-EXEC-001
- **名称**：生成后的基础 pytest 用例应能被 pytest 识别
- **目标**：验证模板生成的测试骨架至少符合 pytest 基本识别规则。
- **预期结果**：pytest 可正常收集该用例。
### TC-V1-EXEC-002
- **名称**：基础 smoke 级执行应可完成
- **目标**：验证 V1 的最小执行链路成立。
- **预期结果**：可调用 pytest 执行；能输出通过/失败结果。
### TC-V1-EXEC-003
- **名称**：执行后应生成基础执行记录
- **目标**：验证执行行为可被记录。
- **检查点**：
 1. 记录执行时间；
 2. 记录执行结果；
 3. 记录报告路径或结果摘要；
 4. 记录执行命令、退出码和测试数量统计。
- **预期结果**：通过。
### TC-V1-EXEC-004
- **名称**：目录级执行应覆盖全部生成测试
- **目标**：验证批量生成多个接口测试时，执行器不会只运行首个测试文件。
- **输入**：包含多个接口操作的 OpenAPI / Swagger 文档。
- **检查点**：
 1. 执行目标为生成测试目录；
 2. junit 报告包含全部生成测试数量；
 3. 执行记录 `target_id` 为 `generated-suite`。
- **预期结果**：通过。
### TC-V1-EXEC-005
- **名称**：CLI 入口应可直接执行文档驱动最小闭环
- **目标**：验证命令行入口可触发解析、生成和执行，并输出执行摘要。
- **输入**：一份本地 OpenAPI / Swagger 文档和输出目录。
- **检查点**：
 1. 进程返回码与执行结果一致；
 2. 标准输出包含 `route_code`、`service_stage`、`workspace_root` 等服务层稳定摘要字段；
 3. 标准输出包含 source、modules、operations、generation_count、asset_count、execution_target、asset_manifest_path 等执行摘要；
 4. 标准输出包含 `execution_exit_code` 与测试数量统计字段；
 5. 输出目录生成接口、测试和记录文件。
- **预期结果**：通过。
### TC-V1-EXEC-006
- **名称**：闭环执行后应生成最小资产清单
- **目标**：验证生成资产不是一次性代码输出，而是具备来源和摘要信息的最小资产集合。
- **输入**：一份可执行的 OpenAPI / Swagger 文档。
- **检查点**：
 1. 生成 `asset_manifest.json`；
 2. 清单中包含 source、generation、execution 关联信息；
 3. 清单中包含资产类型、文件路径和内容摘要。
- **预期结果**：通过。
### TC-V1-EXEC-007
- **名称**：V1 应用服务层应阻止未来路线越界执行
- **目标**：验证 V1 当前仅开放文档驱动闭环，不允许功能用例驱动和抓包驱动直接进入实现。
- **输入**：调用 `functional_case` 或 `traffic_capture` 路由的服务层请求。
- **检查点**：
 1. `document` 路由保持可执行；
 2. `functional_case` 与 `traffic_capture` 返回明确阻止结果；
 3. `describe_capabilities()` 可输出包含阶段信息与阻止原因的能力快照。
- **预期结果**：通过。
### TC-V1-EXEC-008
- **名称**：应用服务层应可检查既有工作区资产清单
- **目标**：验证 V1 最小资产管理边界不仅能落盘，还能通过服务层读取并返回检查结果摘要。
- **输入**：一份已生成并执行过的本地工作区。
- **检查点**：
 1. 能读取 `asset_manifest.json`；
 2. 能返回资产数量、生成记录数量、报告存在状态、资产摘要和生成记录摘要；
 3. 能返回 `command_code`、`service_stage`、`source_type`、`execution_id` 与 `inventory_summary`；
 4. 能识别缺失资产、缺失生成记录或摘要不一致情况。
- **预期结果**：通过。
### TC-V1-EXEC-009
- **名称**：CLI inspect 应可输出工作区资产检查摘要
- **目标**：验证命令行可直接读取既有工作区并输出结构化检查结果。
- **输入**：一个已生成资产清单的工作区目录。
- **检查点**：
 1. 返回码与检查结果一致；
 2. 标准输出包含 `command_code`、`service_stage`、`source_type`、`execution_id`、`validation_status`、`asset_count`、`generation_count` 与资产摘要；
 3. 标准输出同时包含 `inventory_summary`、生成记录摘要、`missing_generation_records` 和问题数量字段；
 4. 对无问题工作区返回有效状态。
- **预期结果**：通过。
### TC-V1-EXEC-010
- **名称**：CLI 与应用服务层不应再暴露旧专用桥接入口
- **目标**：验证通用框架当前只保留文档驱动和工作区检查相关入口，不再提供历史专用桥接命令或方法。
- **输入**：CLI 参数解析器和 `PlatformApplicationService` 实例。
- **检查点**：
 1. CLI 只保留 `run` 与 `inspect` 等当前入口；
 2. 应用服务层不再暴露历史专用桥接方法；
 3. 旧桥接入口无法再作为当前能力被调用。
- **预期结果**：通过。
---
## 10. 最小闭环集成测试用例
## 10.1 目标
验证从文档输入到测试执行的 V1 最小闭环整体可用。
### TC-V1-INT-001
- **名称**：文档驱动最小闭环集成测试
- **目标**：验证平台可从文档输入打通到测试骨架生成与执行。
- **步骤**：
 1. 输入一份结构化 OpenAPI / Swagger 文档；
 2. 解析为中间模型；
 3. 生成接口方法；
 4. 生成基础 pytest 测试骨架；
 5. 通过基础规则校验；
 6. 执行 pytest；
 7. 记录生成与执行结果。
- **预期结果**：闭环成立。
### TC-V1-INT-002
- **名称**：闭环结果应具备最小可追溯性
- **目标**：验证最小闭环完成后，资产不是一次性产物。
- **检查点**：
 1. 有来源信息；
 2. 有生成记录；
 3. 有执行记录；
 4. 有目标文件定位信息；
 5. 有最小资产清单。
- **预期结果**：通过。
### TC-V1-INT-003
- **名称**：解析层应支持 YAML 格式 OpenAPI 文档
- **目标**：验证解析器可直接处理 YAML 输入，而不是仅支持 JSON。
- **输入**：一份 YAML 格式的 OpenAPI 3.x 文档。
- **检查点**：
 1. 文档可被成功解析；
 2. `source_type` 识别为 `openapi`；
 3. 生成的操作对象命名和字段映射正确。
- **预期结果**：通过。
### TC-V1-INT-004
- **名称**：解析层应支持 Swagger YAML 多接口文档
- **目标**：验证解析器可处理 Swagger 2.0 YAML，并正确产出多个接口操作。
- **输入**：一份包含多个接口的 Swagger YAML 文档。
- **检查点**：
 1. 文档可被成功解析；
 2. `source_type` 识别为 `swagger`；
 3. 多个接口操作都能映射为中间模型对象。
- **预期结果**：通过。
### TC-V1-INT-005
- **名称**：文档驱动闭环应支持对象数组结构断言
- **目标**：验证对象数组响应结构进入文档驱动闭环后，生成测试骨架仍可执行通过。
- **输入**：一份返回对象数组的 OpenAPI 文档。
- **检查点**：
 1. 生成测试骨架包含数组项结构断言；
 2. 生成测试骨架包含对象数组假响应；
 3. pytest 执行结果为通过。
- **预期结果**：通过。
---
## 11. V1 测试优先级建议
### P0（必须覆盖）
- 文档约束测试
- 核心中间模型测试
- 接口模板生成测试
- pytest 用例模板测试
- 基础规则校验测试
- 最小闭环集成测试
### P1（建议尽快补齐）
- 更复杂 `business_rule` DSL，以及更深层多层嵌套对象/数组结构测试
### P2（V2 逐步增强）
- 场景驱动测试
- 变量绑定测试
- 依赖编排测试
- 抓包驱动闭环测试
---
## 12. 测试通过标准
V1 阶段，以下条件同时满足时，才能宣称 V1 最小闭环通过：
1. 所有 P0 测试项完成设计并通过；
2. 中间模型核心对象可以稳定承接输入；
3. 模板层可生成统一风格资产；
4. 规则层能拦截明显不合规结果；
5. pytest 最小执行链路成立；
6. 生成与执行记录具备最小可追溯性。
---
## 13. 当前测试设计进度记录
| 编号 | 测试设计项 | 状态 | 说明 | 最近更新 |
|------|------------|------|------|----------|
| TDD-V1-001 | V1 测试设计文档创建 | 已完成 | 已建立 V1 详细测试说明文档第一版 | 2026-03-30 |
| TDD-V1-002 | 文档约束测试设计 | 已完成 | 已定义 TC-V1-DOC-* 系列 | 2026-03-30 |
| TDD-V1-003 | 中间模型测试设计 | 已完成（当前轮增强） | 已定义 TC-V1-MODEL-* 系列，并补充 `TC-V1-MODEL-006A` / `TC-V1-MODEL-007A` / `TC-V1-MODEL-008` / `TC-V1-MODEL-009` / `TC-V1-MODEL-010A` / `TC-V1-MODEL-010`，覆盖生成记录资产摘要、执行记录结构化统计、路线能力快照、文档驱动运行摘要、工作区资产聚合摘要和工作区检查摘要模型 | 2026-04-02 |
| TDD-V1-004 | 模板生成测试设计 | 已完成（当前轮增强） | 已定义 TC-V1-TPL-* 系列，并在当前轮补充 `TC-V1-TPL-002A` / `TC-V1-TPL-002B` / `TC-V1-TPL-002C` / `TC-V1-TPL-002D` / `TC-V1-TPL-002E` / `TC-V1-TPL-003A` / `TC-V1-TPL-003B` / `TC-V1-TPL-003C` / `TC-V1-TPL-003D`，覆盖 `schema_match`、`business_rule(non_empty_string/positive_integer)`、对象数组结构断言与按断言生成假响应体的模板约束 | 2026-04-02 |
| TDD-V1-005 | 规则层测试设计 | 已完成（当前轮增强） | 已定义 TC-V1-RULE-* 系列，并在当前轮补充 `TC-V1-RULE-011` / `TC-V1-RULE-012` / `TC-V1-RULE-013`，覆盖 `schema_match` 最小结构校验、对象数组 `item_type` 规则校验，以及 `business_rule.rule_code` 最小校验 | 2026-04-02 |
| TDD-V1-006 | 执行层测试设计 | 已完成（当前轮增强） | 已定义 TC-V1-EXEC-* 系列，并补充 CLI 资产清单输出、最小资产清单落盘、工作区检查、执行统计摘要、服务层能力快照、工作区检查服务摘要、资产聚合摘要与 V1 应用服务边界场景 | 2026-04-02 |
| TDD-V1-007 | 集成闭环测试设计 | 已完成（当前轮增强） | 已定义 TC-V1-INT-* 系列，并补充 YAML / Swagger 输入兼容性、资产追溯、执行统计、工作区检查、服务边界场景、`schema_match` 落地后“模板假响应与断言保持一致”的闭环约束，以及对象数组结构断言闭环场景 `TC-V1-INT-005` | 2026-04-02 |
| TDD-V1-008 | 测试实现与落地 | 已完成（V1验收通过） | 已在当前重构分支完成 `api_test`、`platform_core` 与根治理测试的全部落地，覆盖 `api_config.json` 唯一配置源、代理配置字段、`BaseAPI/common_tools` 治理、`schema_match`、`business_rule(non_empty_string/positive_integer)`、工作区资产聚合摘要、依赖治理与公开基线回归；最终验收结果为 `63 passed` / `70 passed` / `39 passed` / `12 passed, 27 deselected` | 2026-04-02 |
| TDD-V1-009 | 对象数组结构断言增强综合回归 | 已完成（当前轮） | 当前重构分支已执行 `python -m pytest tests/platform_core/test_templates_and_rules.py tests/platform_core/test_services_and_assets.py tests/platform_core/test_pipeline.py -k "array_object or schema_match" -v --basetemp .pytest_tmp/array_schema_green`，结果 `8 passed`；已执行 `python -m pytest tests/platform_core -v --basetemp .pytest_tmp/platform_core_array_schema_full`，结果 `48 passed`；已执行 `python -m pytest tests -v --basetemp .pytest_tmp/root_array_schema_full`，结果 `53 passed`；已执行 `python -m pytest api_test/tests -v --basetemp .pytest_tmp/api_test_array_schema_full`，结果 `30 passed`；已执行 `python api_test/run_test.py --public-baseline` 与 `cd api_test && python run_test.py --public-baseline`，结果均为 `12 passed, 18 deselected` | 2026-04-02 |
| TDD-V1-015 | BaseAPI 职责收口与通用工具迁移回归 | 已完成（当前轮） | 已执行 `python -m pytest api_test/tests/test_base_api_governance.py api_test/tests/test_common_tools.py -v --noconftest --basetemp .pytest_tmp/base_api_split_docs`，结果 `19 passed`；已执行 `python -m pytest api_test/tests -v --basetemp .pytest_tmp/api_test_base_api_split_full`，结果 `39 passed`；已执行 `python -m pytest tests/platform_core -v --basetemp .pytest_tmp/platform_core_after_base_api_split`，结果 `48 passed`；已执行 `python -m pytest tests -v --basetemp .pytest_tmp/root_after_base_api_split`，结果 `53 passed`；已执行 `python api_test/run_test.py --public-baseline` 与 `cd api_test && python run_test.py --public-baseline`，结果均为 `12 passed, 27 deselected` | 2026-04-02 |
| TDD-V1-016 | 服务摘要契约与 CLI 服务化输出测试设计 | 已完成（当前轮） | 已新增服务层能力快照与文档驱动运行摘要模型测试，完成 `PlatformApplicationService.describe_capabilities()`、`run_document_pipeline_summary()` 与 CLI `run` 服务化输出验证；当前轮补齐 `source_type`、`execution_id` 和 `asset_type_breakdown` 后，最终验收全量结果为 `63 passed` / `70 passed` / `39 passed` / `12 passed, 27 deselected` | 2026-04-02 |
| TDD-V1-017 | 工作区检查摘要契约与 CLI inspect 服务化输出测试设计 | 已完成（当前轮） | 已新增 `WorkspaceInspectionSummary`、`WorkspaceAssetInventorySummary`、`inspect_workspace_summary()` 与 CLI `inspect` 服务化输出测试；当前轮补齐 `source_type`、`execution_id` 和 `inventory_summary` 后，最终验收全量结果为 `63 passed` / `70 passed` / `39 passed` / `12 passed, 27 deselected` | 2026-04-02 |
| TDD-V1-018 | `business_rule` 最小闭环测试设计 | 已完成（最终收口轮） | 已按 TDD 先执行 `python -m pytest tests/platform_core/test_models.py tests/platform_core/test_templates_and_rules.py tests/platform_core/test_services_and_assets.py tests/test_dependency_governance.py -k "positive_integer or inventory_summary or source_type or execution_id or asset_type_breakdown or dependency_governance" -v --basetemp .pytest_tmp/v1_final_closure_red`，结果 `1 error`；随后完成 `business_rule(positive_integer)`、资产聚合摘要和依赖治理实现，并执行同口径绿灯验证 `6 passed`；已完成 `63 passed` / `70 passed` / `39 passed` / `12 passed, 27 deselected` 的最终验收回归 | 2026-04-02 |
| TDD-V1-019 | 依赖安全治理测试设计 | 已完成（当前轮） | 已新增 `tests/test_dependency_governance.py`，锁定 `api_test/requirements.txt` 固定版本约束与未使用 `rsa` 删除要求，并完成定向与全量回归验证 | 2026-04-02 |
| TDD-V1-014 | 中文注释与配置说明治理测试设计 | 已完成 | 已新增 `tests/test_comment_governance.py`，用于校验仓库内 Python 模块/类/方法中文 docstring 和 `api_config.json` 中文说明字段；同时补充 `api_test/tests/test_config_loader.py::test_load_api_config_ignores_comment_fields`，锁定配置加载器对 `_comment` / `*_comment` 字段的兼容性 | 2026-04-01 |
| TDD-V1-010 | 资产清单检查能力测试设计 | 已完成（当前轮增强） | 已新增 `TC-V1-RULE-008`、`TC-V1-EXEC-008` 与 `TC-V1-EXEC-009`，完成 `tests/platform_core/test_services_and_assets.py` 定向复验，并在当前轮补齐生成记录摘要输出、缺失生成记录识别、工作区检查服务摘要、问题数量统计和 CLI inspect 结构化输出验证 | 2026-04-02 |
| TDD-V1-011 | JSONPlaceholder 公开基线测试设计 | 已完成（综合验证） | 已新增公开站点规则与 JSONPlaceholder 测试方法约束，完成 `api_test` 定向验证 `13 passed`、全量回归 `30 passed, 4 skipped` 和公开基线回归 `30 passed, 4 deselected`，覆盖 CRUD、过滤、嵌套路由和 REST 状态码兼容 | 2026-03-31 |
| TDD-V1-012 | JSONPlaceholder 资源级底座测试设计 | 已完成（第四轮） | 已新增 `put/patch` 便捷方法、公共 fixture、users 资源和 todos 过滤/嵌套路由测试，并完成定向验证 `13 passed` 与全量回归 | 2026-03-31 |
| TDD-V1-013 | 通用框架清理治理测试设计 | 已完成 | 已将原历史桥接相关测试改写为通用治理测试，覆盖旧专用文件删除、规则层入口收口、CLI/服务层入口收口和主文档状态同步 | 2026-04-01 |
---
## 14. 后续建议
基于当前说明书，下一步建议转入 V2：
1. 继续扩展更复杂 `business_rule` DSL；
2. 继续扩展更深层结构断言、变量绑定与依赖编排；
3. 在新的阶段文档中继续按 TDD 流程推进实现与验收。
---
## 15. 结论
本说明书定义的 V1 测试目标已经全部落地，并完成最终验收。
截至 2026-04-02，V1 可宣称：
- 最小闭环通过
- P0 全部完成并通过
- 当前阶段开发与测试验收完成
---
> 备注：本说明书现作为 V1 阶段验收版保留；后续新增能力应转入 V2 阶段测试说明书继续扩展。
